#include <schreiben_lesen.h>

int main (void) {
   MYSQL  *mysql = mysql_init(NULL);						// Handle initialisieren
   struct CGI_DATEN *messdaten = new CGI_DATEN();
   double rg_wert;

   schreibe_in_db(mysql, "SmartWindow", "ManOpen", messdaten, "0");
   schreibe_in_db(mysql, "SmartWindow", "InTemp", messdaten, "54.987");

//   schreibe_in_db(mysql, "SmartWindow", "OutTemp", messdaten, "20.0");
//   schreibe_in_db(mysql, "SmartWindow", "MaxTemp", messdaten, "20.0");
//   schreibe_in_db(mysql, "SmartWindow", "MinHum", messdaten, "20.0");
//   schreibe_in_db(mysql, "SmartWindow", "MaxHum", messdaten, "20.0");
//   schreibe_in_db(mysql, "SmartWindow", "MaxAir", messdaten, "20.0");
//   schreibe_in_db(mysql, "SmartWindow", "MaxWind", messdaten, "20.0");
//   schreibe_in_db(mysql, "SmartWindow", "MaxSound", messdaten, "20.0");

   rg_wert = daten_suchen(mysql, "SmartWindow", "ManOpen", messdaten);
//   rg_wert = daten_suchen(mysql, "SmartWindow", "Modus", messdaten);
//  rg_wert = daten_suchen(mysql, "SmartWindow", "ManClose", messdaten);

   printf("Der Wert betraegt: %f\n", rg_wert);
   return 0;
}



